import { Routes } from '@angular/router';
import { MailgunComponent } from './mailgun/mailgun.component';

export const Router: Routes = [
    {
        path: '',
        component: MailgunComponent
    }
]
